# ☁️ ضبابة الحمراء (Red Cloud Storage)

**حل تخزين سحابي ذاتي الاستضافة ومتوافق مع S3 (MinIO) مع تأمين تلقائي عبر Caddy.**

هذا المستودع يحتوي على ملفات الإعداد اللازمة لنشر خدمة تخزين سحابي خاصة بك على خادم افتراضي خاص (VPS)، مما يمنحك تحكماً كاملاً في بياناتك.

## 🚀 المكونات الأساسية

| المكون | الوصف |
| :--- | :--- |
| **MinIO** | محرك تخزين متوافق مع واجهة برمجة تطبيقات Amazon S3. |
| **Caddy** | خادم ويب ووكيل عكسي (Reverse Proxy) يوفر شهادات HTTPS (TLS) تلقائياً عبر Let's Encrypt. |
| **Docker Compose** | لأتمتة عملية النشر وتشغيل الخدمات في حاويات معزولة. |

## 🛠️ الإعداد (Setup)

### 1. المتطلبات المسبقة

تأكد من توفر ما يلي على الـ VPS الخاص بك:

*   **خادم افتراضي خاص (VPS):** يعمل بنظام Linux (مثل Ubuntu).
*   **اسم نطاق (Domain Name):** نطاق فرعي موجه إلى عنوان IP الخاص بالـ VPS (مثلاً: `storage.yourdomain.com`).
*   **Docker و Docker Compose:** يجب تثبيتهما على الـ VPS.

### 2. استنساخ المستودع

قم بتسجيل الدخول إلى الـ VPS الخاص بك واستنسخ هذا المستودع:

```bash
git clone https://github.com/badreddine023/red-cloud-storage.git
cd red_cloud_storage
```

### 3. تعديل ملفات الإعداد

**أ. `docker-compose.yml`**

**مهم جداً:** قم بتغيير بيانات الاعتماد الافتراضية في قسم `environment` لـ `minio` إلى قيم قوية وآمنة:

```yaml
    environment:
      MINIO_ROOT_USER: YOUR_SECURE_ACCESS_KEY
      MINIO_ROOT_PASSWORD: YOUR_VERY_STRONG_SECRET_KEY
```

**ب. `Caddyfile`**

**مهم جداً:** استبدل `storage.example.com` باسم النطاق الفعلي الذي قمت بتوجيهه إلى الـ VPS:

```caddy
storage.example.com {
    reverse_proxy minio:9000
}
```

## 🌐 تشغيل HTTPS (Launch HTTPS)

بمجرد تعديل الملفات، يمكنك تشغيل الخدمة بالكامل. سيتولى Caddy مهمة الحصول على شهادة SSL/TLS تلقائياً وتفعيل HTTPS.

1.  **إنشاء مجلد البيانات:**
    ```bash
    mkdir minio_data
    ```

2.  **تشغيل الخدمات:**
    ```bash
    sudo docker compose up -d
    ```

3.  **التحقق من حالة الخدمات:**
    ```bash
    sudo docker compose ps
    ```
    يجب أن تكون حالة كل من `red_cloud_minio` و `red_cloud_caddy` هي `Up`.

## 🔗 الوصول إلى التخزين

يمكنك الآن الوصول إلى التخزين السحابي الخاص بك عبر متصفح الويب:

*   **نقطة النهاية (S3 Endpoint):** `https://storage.yourdomain.com`
*   **واجهة التحكم (MinIO Console):** `https://storage.yourdomain.com:9001` (إذا لم تستخدم Caddy لواجهة التحكم)

استخدم بيانات الاعتماد التي قمت بتعيينها في `docker-compose.yml` لتسجيل الدخول.

## 🐍 مثال استخدام Python

للتفاعل البرمجي مع التخزين، يمكنك استخدام ملف `minio_python_example.py` بعد تثبيت مكتبة `boto3` وتعديل القيم بداخله لتناسب النطاق وكلمات السر الجديدة.

```bash
pip install boto3
python3 minio_python_example.py
```

---

**ملاحظة:** تم توفير دليل مفصل باللغة العربية في ملف `deployment_guide.md`.
